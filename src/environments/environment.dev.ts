export const environment = {
    production: false,
    fyle_url: $ENV.FYLE_URL,
    fyle_app_url: $ENV.FYLE_APP_URL,
    fyle_client_id: $ENV.FYLE_CLIENT_ID,
    callback_uri: $ENV.CALLBACK_URI,
    api_url: $ENV.API_URL,
    app_url: $ENV.APP_URL,
    qbo_client_id: $ENV.QBO_CLIENT_ID,
    qbo_scope: $ENV.QBO_SCOPE,
    qbo_authorize_uri: $ENV.QBO_AUTHORIZE_URI,
    qbo_app_url: $ENV.QBO_APP_URL,
    hotjar_id: $ENV.HOTJAR_ID,
    sentry_dsn: $ENV.SENTRY_DSN,
    release: $ENV.RELEASE,
    new_qbo_app_url: $ENV.NEW_QBO_APP_URL
};
